# ext-assetus

Public contract repository for Assetus. `frontend/` is the sole publisher of
`@sneat/extension-assetus-contract`; the paired `assetus` repository owns the
runtime package and standalone application.
